import { StyleSheet } from 'react-native';

const productDetailStyles = StyleSheet.create({
    container: {
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center'
    }
});

export { productDetailStyles };